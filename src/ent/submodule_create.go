// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/0B1t322/CP-Rosseti-Back/ent/module"
	"github.com/0B1t322/CP-Rosseti-Back/ent/submodule"
	"github.com/0B1t322/CP-Rosseti-Back/ent/submoduletest"
)

// SubModuleCreate is the builder for creating a SubModule entity.
type SubModuleCreate struct {
	config
	mutation *SubModuleMutation
	hooks    []Hook
}

// SetName sets the "name" field.
func (smc *SubModuleCreate) SetName(s string) *SubModuleCreate {
	smc.mutation.SetName(s)
	return smc
}

// SetText sets the "text" field.
func (smc *SubModuleCreate) SetText(s string) *SubModuleCreate {
	smc.mutation.SetText(s)
	return smc
}

// SetModuleID sets the "Module" edge to the Module entity by ID.
func (smc *SubModuleCreate) SetModuleID(id int) *SubModuleCreate {
	smc.mutation.SetModuleID(id)
	return smc
}

// SetNillableModuleID sets the "Module" edge to the Module entity by ID if the given value is not nil.
func (smc *SubModuleCreate) SetNillableModuleID(id *int) *SubModuleCreate {
	if id != nil {
		smc = smc.SetModuleID(*id)
	}
	return smc
}

// SetModule sets the "Module" edge to the Module entity.
func (smc *SubModuleCreate) SetModule(m *Module) *SubModuleCreate {
	return smc.SetModuleID(m.ID)
}

// SetTestID sets the "Test" edge to the SubModuleTest entity by ID.
func (smc *SubModuleCreate) SetTestID(id int) *SubModuleCreate {
	smc.mutation.SetTestID(id)
	return smc
}

// SetNillableTestID sets the "Test" edge to the SubModuleTest entity by ID if the given value is not nil.
func (smc *SubModuleCreate) SetNillableTestID(id *int) *SubModuleCreate {
	if id != nil {
		smc = smc.SetTestID(*id)
	}
	return smc
}

// SetTest sets the "Test" edge to the SubModuleTest entity.
func (smc *SubModuleCreate) SetTest(s *SubModuleTest) *SubModuleCreate {
	return smc.SetTestID(s.ID)
}

// Mutation returns the SubModuleMutation object of the builder.
func (smc *SubModuleCreate) Mutation() *SubModuleMutation {
	return smc.mutation
}

// Save creates the SubModule in the database.
func (smc *SubModuleCreate) Save(ctx context.Context) (*SubModule, error) {
	var (
		err  error
		node *SubModule
	)
	if len(smc.hooks) == 0 {
		if err = smc.check(); err != nil {
			return nil, err
		}
		node, err = smc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*SubModuleMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = smc.check(); err != nil {
				return nil, err
			}
			smc.mutation = mutation
			if node, err = smc.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(smc.hooks) - 1; i >= 0; i-- {
			if smc.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = smc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, smc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (smc *SubModuleCreate) SaveX(ctx context.Context) *SubModule {
	v, err := smc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (smc *SubModuleCreate) Exec(ctx context.Context) error {
	_, err := smc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (smc *SubModuleCreate) ExecX(ctx context.Context) {
	if err := smc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (smc *SubModuleCreate) check() error {
	if _, ok := smc.mutation.Name(); !ok {
		return &ValidationError{Name: "name", err: errors.New(`ent: missing required field "name"`)}
	}
	if _, ok := smc.mutation.Text(); !ok {
		return &ValidationError{Name: "text", err: errors.New(`ent: missing required field "text"`)}
	}
	return nil
}

func (smc *SubModuleCreate) sqlSave(ctx context.Context) (*SubModule, error) {
	_node, _spec := smc.createSpec()
	if err := sqlgraph.CreateNode(ctx, smc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{err.Error(), err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (smc *SubModuleCreate) createSpec() (*SubModule, *sqlgraph.CreateSpec) {
	var (
		_node = &SubModule{config: smc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: submodule.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: submodule.FieldID,
			},
		}
	)
	if value, ok := smc.mutation.Name(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: submodule.FieldName,
		})
		_node.Name = value
	}
	if value, ok := smc.mutation.Text(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: submodule.FieldText,
		})
		_node.Text = value
	}
	if nodes := smc.mutation.ModuleIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   submodule.ModuleTable,
			Columns: []string{submodule.ModuleColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: module.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.module_sub_modules = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	if nodes := smc.mutation.TestIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2O,
			Inverse: false,
			Table:   submodule.TestTable,
			Columns: []string{submodule.TestColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: submoduletest.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// SubModuleCreateBulk is the builder for creating many SubModule entities in bulk.
type SubModuleCreateBulk struct {
	config
	builders []*SubModuleCreate
}

// Save creates the SubModule entities in the database.
func (smcb *SubModuleCreateBulk) Save(ctx context.Context) ([]*SubModule, error) {
	specs := make([]*sqlgraph.CreateSpec, len(smcb.builders))
	nodes := make([]*SubModule, len(smcb.builders))
	mutators := make([]Mutator, len(smcb.builders))
	for i := range smcb.builders {
		func(i int, root context.Context) {
			builder := smcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*SubModuleMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, smcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, smcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{err.Error(), err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, smcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (smcb *SubModuleCreateBulk) SaveX(ctx context.Context) []*SubModule {
	v, err := smcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (smcb *SubModuleCreateBulk) Exec(ctx context.Context) error {
	_, err := smcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (smcb *SubModuleCreateBulk) ExecX(ctx context.Context) {
	if err := smcb.Exec(ctx); err != nil {
		panic(err)
	}
}
